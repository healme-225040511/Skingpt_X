# --- 核心深度学习框架 ---
torch>=2.2.0
torchvision>=0.17.0
# Qwen3-VL 必须使用最新的 transformers
git+https://github.com/huggingface/transformers
accelerate>=0.30.0
sentencepiece
protobuf

# --- Qwen3-VL 专用工具 ---
qwen_vl_utils>=0.0.4
pillow
# 如果需要高性能推理建议安装 (可选，需要编译环境)
# flash-attn --no-build-isolation 

# --- RAG & 向量数据库 (LlamaIndex + LanceDB) ---
llama-index>=0.10.0
llama-index-vector-stores-lancedb
llama-index-embeddings-huggingface
lancedb>=0.6.0
pyarrow

# --- 知识图谱 & 数据库 ---
neo4j>=5.10.0

# --- 数据处理与数学计算 ---
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.2.0
thefuzz  # 用于字符串模糊匹配诊断标签

# --- 辅助工具 ---
tqdm
pathlib
python-dotenv
agno  # 你代码中用到的 Agent 框架

markdown
aiofiles